export const DaysCalendar = ({ index }: { index: number }) => {
    return (
        <td
        key={index}
        className="h-12 hover:bg-indigo-100 transition-all duration-200 cursor-pointer"
      ></td>
    )
}
